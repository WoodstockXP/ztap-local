apiVersion: apps/v1
kind: Deployment
metadata:
  name: ztap-gateway
  namespace: enforcer-a
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ztap-gateway
      tenant: tenant-a
  template:
    metadata:
      labels:
        app: ztap-gateway
        tenant: tenant-a
    spec:
      nodeSelector:
        ztap.io/node-pool: tenant-a
      containers:
        - name: ztap-gateway
          image: ztap-app:local
          imagePullPolicy: IfNotPresent
          command: ["uvicorn", "gateway.main:app", "--host", "0.0.0.0", "--port", "8001"]
          env:
            - name: ZTAP_KEYCLOAK_ISSUER
              value: http://keycloak.authorizer-a.svc.cluster.local:8080/realms/ztap-tenant-a
            - name: ZTAP_CLIENT_ID
              value: ztap-gateway
            - name: ZTAP_AGENT_ID
              value: invoice-agent-v2
          ports:
            - containerPort: 8001
---
apiVersion: v1
kind: Service
metadata:
  name: ztap-gateway
  namespace: enforcer-a
spec:
  selector:
    app: ztap-gateway
    tenant: tenant-a
  ports:
    - port: 8001
      targetPort: 8001
